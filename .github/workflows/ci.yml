name: 🚀 Quality & Delivery

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ------------------------------------------------------------------
  # JOB 1: GLOBAL AUDIT (Security)
  # ------------------------------------------------------------------
  global-audit:
    name: 🔒 Global Audit
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: '**/package-lock.json'

      - name: 🔍 Audit Frontend
        working-directory: ./frontend
        run: |
          npm ci --legacy-peer-deps --ignore-scripts
          npm audit --audit-level=high || true

      - name: 🔍 Audit Backend
        working-directory: ./backend
        run: |
          npm ci --ignore-scripts
          npm audit --audit-level=high || true

  # ------------------------------------------------------------------
  # JOB 2: BACKEND QUALITY (Lint & Build)
  # ------------------------------------------------------------------
  backend-quality:
    name: 🛡️ Backend Quality
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: 📦 Install Dependencies
        working-directory: ./backend
        run: npm ci

      - name: 🎨 Linting (ESLint)
        working-directory: ./backend
        run: npm run lint

      - name: 🏗️ Build NestJS
        working-directory: ./backend
        run: |
          npx prisma generate
          npm run build

  # ------------------------------------------------------------------
  # JOB 3: BACKEND TESTS (Jest)
  # ------------------------------------------------------------------
  backend-tests:
    name: 🧪 Backend Tests
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: 📦 Install Dependencies
        working-directory: ./backend
        run: npm ci

      - name: 🧪 Unit Tests (Jest)
        working-directory: ./backend
        run: |
          npx prisma generate
          npm run test

  # ------------------------------------------------------------------
  # JOB 4: FRONTEND QUALITY (Lint & Build)
  # ------------------------------------------------------------------
  frontend-quality:
    name: 🎨 Frontend Quality
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Dependencies
        working-directory: ./frontend
        run: npm ci --legacy-peer-deps

      - name: 🧬 Prisma Generate
        working-directory: ./frontend
        run: npx prisma generate --schema=prisma/schema.prisma

      - name: 🎨 Linting (ESLint)
        working-directory: ./frontend
        run: npm run lint

      - name: 🏗️ Build Next.js
        working-directory: ./frontend
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: 1

  # ------------------------------------------------------------------
  # JOB 5: FRONTEND TESTS (Jest)
  # ------------------------------------------------------------------
  frontend-tests:
    name: 🧪 Frontend Tests
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📦 Install Dependencies
        working-directory: ./frontend
        run: npm ci --legacy-peer-deps

      - name: 🧬 Prisma Generate
        working-directory: ./frontend
        run: npx prisma generate --schema=prisma/schema.prisma

      - name: 🧪 Unit Tests (Jest)
        working-directory: ./frontend
        run: npm run test

  # ------------------------------------------------------------------
  # JOB 6: SECURITY SCAN & DOCKER BUILD
  # ------------------------------------------------------------------
  security-and-build:
    name: 🔒 Security & Docker Build
    runs-on: ubuntu-latest
    needs: [global-audit, backend-quality, backend-tests, frontend-quality, frontend-tests]
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🐳 Build Frontend Image
        run: docker build -f frontend/Dockerfile -t collector-app:latest .

      - name: 🔍 Trivy Scan (Frontend)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'collector-app:latest'
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

      - name: 🐳 Build Backend Image
        run: docker build -f backend/Dockerfile -t collector-backend:latest ./backend

      - name: 🔍 Trivy Scan (Backend)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'collector-backend:latest'
          format: 'table'
          exit-code: '0'
          ignore-unfixed: true
          vuln-type: 'os,library'
          severity: 'CRITICAL,HIGH'

  # ------------------------------------------------------------------
  # JOB 7: DELIVERY (Simulation)
  # ------------------------------------------------------------------
  deploy-simulation:
    name: 🚀 Deploy Simulation
    runs-on: ubuntu-latest
    needs: security-and-build
    if: github.event_name == 'push'
    environment:
      name: ${{ github.ref == 'refs/heads/main' && 'production' || 'recette' }}
      url: ${{ github.ref == 'refs/heads/main' && 'https://collector.app' || 'https://recette.collector.app' }}
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4
      
      - name: 🚚 Deploy Simulation
        run: |
          echo "==== [FAKE DEPLOYMENT] ===="
          echo "Target Environment: ${{ github.ref == 'refs/heads/main' && 'Production' || 'Recette' }}"
          echo "1. Terraform Init/Apply (Simulated)..."
          echo ">> terraform apply -auto-approve"
          echo "2. Deployment Success!"
